/**
 * Drizzle schema for the Recogno Postgres database.
 *
 * Intentionally empty: this scaffold ships no feature tables yet. Add table
 * definitions here (users, decks, cards, reviews, ...) and run `pnpm db:generate`
 * to produce a migration under `packages/shared/drizzle/`.
 */

export {};
