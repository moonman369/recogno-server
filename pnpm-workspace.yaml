packages:
  - "packages/*"

# pnpm >=10 blocks lifecycle scripts by default; opt in explicitly.
allowBuilds:
  # tsup's bundler needs its platform binary.
  esbuild: true
  # Optional native accelerator for msgpackr (via bullmq); the JS fallback is fine.
  msgpackr-extract: false
